PORT=3000
JWT_SECRET=

# Development (docker compose --profile dev up)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/panorama_dev

# QA (docker compose --profile qa up -- used by Docker Compose internally)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5433/panorama_qa
